# 🧠 Персональний Помічник (Address Book & Notes)

Персональний помічник — це застосунок командного рядка для зручної роботи з **контактами** та **нотатками**.  
Він дозволяє зберігати та швидко знаходити інформацію, редагувати її і зберігати між запусками.

---

## ✨ Функціонал

### 📇 Контакти
- Зберігання:
  - Імені
  - Телефону (з валідацією)
  - Email (з валідацією)
  - Адреси
  - Дня народження
- Пошук за різними параметрами
- Редагування та видалення контактів
- Вивід контактів, у яких **ДН через задану кількість днів**

### 📝 Нотатки
- Додавання нотаток
- Редагування та видалення
- Додавання тегів
- Пошук за текстом або тегами
- Фільтрація нотаток за тегом

### 🤖 Підказка схожих команд
При неправильному введенні команда пропонується автоматично.

### 💾 Збереження даних
Всі дані зберігаються у домашньому каталозі користувача:

```
~/.personal_assistant/
```

---

## 🧩 Архітектура проєкту

```
project/
├─ src/
│ ├─ personal_assistant/
│ │ ├─ init.py
│ │ ├─ addressbook.py # Класи Field, Record, AddressBook
│ │ ├─ notes.py # Класи Note та NotesBook
│ │ ├─ storage.py # Збереження/відновлення даних (pickle + міграція)
│ │ ├─ command_handler.py # Обробка логіки CLI-команд
│ │ └─ main.py # Основний CLI-інтерфейс (точка входу)
│
├─ README.md # Опис проєкту та інструкції
├─ requirements.txt # Список залежностей
├─ pyproject.toml # Конфігурація Python-пакета (entry point → assistant)
└─ .gitignore # Ігноровані файли Git
```

---

## 🛠 Встановлення та запуск

> Потрібен Python **3.10+**

1. Клонування репозиторію:
```bash
git clone https://github.com/nazar03uk/personal-assistant-pg-20.git
cd personal-assistant-pg-20
```

2. Встановлення в режимі розробки:
```bash
pip install -e .
```

3. Запуск програми в терміналі (Linux, MacOS, Windows):
```bash
assistant
```

---

## 💬 Команди CLI

| Команда | Опис |
|--------|------|
| `add` | Створити новий контакт |
| `add-address` | Додати/змінити адресу |
| `email` | Додати/змінити email |
| `add-birthday` | Додати/змінити день народження |
| `edit-phone` | Змінити номер телефону |
| `remove-phone` | Видалити номер телефону |
| `delete` | Видалити контакт |
| `show` | Показати всі контакти |
| `show-contact` | Показати конкретний контакт |
| `find` | Пошук контактів |
| `birthdays` | Показати ДН у найближчі дні |
| `add-note` | Додати нотатку |
| `edit-note` | Редагувати нотатку |
| `delete-note` | Видалити нотатку |
| `add-tag` | Додати тег |
| `remove-tag` | Видалити тег |
| `find-note` | Пошук нотаток |
| `show-notes` | Показати всі нотатки |
| `show-notes-by-tag` | Показати нотатки з тегом |
| `help` | Список доступних команд |
| `exit` / `close` | Вихід із збереженням |

---

## ✅ Стандарти якості
- ООП-підхід
- Валідація даних у відповідних класах
- Обробка помилкових вводів
- Дотримано стандарту **PEP 8**
- Дані не втрачаються між запусками

---

## 👨‍💻 Команда розробників

| Ім’я | Email |
|-----|-------|
| **Nazar Marchuk** | nazar03uk@gmail.com |
| **Oksana Melchenko** | oksankajk@gmail.com |
| **Svjatoslav Cherednikov** | svjatoslav.cherednikov@gmail.com |
| **Vikram Ahluwalia** | dr.vikram.ahluwalia@gmail.com |
| **Dmytro Riabichenko** | riabichenko@protonmail.com |

---

Слава Україні! 💛💙
